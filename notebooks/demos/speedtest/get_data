#!/usr/bin/env zsh

turns=40
samples=5

speedtest --csv-header > speed.csv # create header.

repeat $turns {
  # start speedtest marathon, this might take a while:
  repeat $samples {speedtest --server 5351 --csv >> speed.csv}
  repeat $samples {speedtest --server 1738 --csv >> speed.csv}
  repeat $samples {speedtest --server 6260 --csv >> speed.csv}
}
